# Phase 4: Exceptions

| Item | What | Exception | Reason |
|------|------|-----------|--------|
| Client sidebar nav | "Submit Records" links to `/portal/upload` (stub) instead of `/portal/submit` (working wizard) | Routing mismatch | Sidebar nav link needs updating to point to the correct route. Non-blocking — wizard works at `/portal/submit`. |
